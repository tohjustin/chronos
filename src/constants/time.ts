import { milliseconds } from "../utils/dateUtils";

export const MS_PER_SECOND = milliseconds({ second: 1 });
export const MS_PER_MINUTE = milliseconds({ minute: 1 });
export const MS_PER_HOUR = milliseconds({ hour: 1 });
export const MS_PER_DAY = milliseconds({ day: 1 });
